import { primary, secondary } from './colors';

const palette = {
  palette: {
    mode: 'dark',

    primary: {
      main: primary,
    },

    secondary: {
      main: secondary,
    },
  },
};

export default palette;
